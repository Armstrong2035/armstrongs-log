import Link from "next/link";
import { Nav, Footer } from "../components";
import { essays, projectLogs, writingGroups, formatDate } from "../content";
export default function Writing(){const all=[...essays,...projectLogs].sort((a,b)=>b.date.localeCompare(a.date));return <main><Nav/><section className="innerHero shell"><p className="sectionLabel">Writing archive</p><h1>Essays, systems, and <em>ideas in progress.</em></h1><p>The complete collection, preserved from Armstrong&apos;s original Markdown archive.</p></section><section className="pageBody shell"><div className="collectionNav"><a href="#chronological">Master index</a>{Object.keys(writingGroups).map((group)=><Link href={`/writing/series/${group.toLowerCase().replace(/&/g,"and").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")}`} key={group}>{group}</Link>)}</div><div className="essayList" id="chronological">{all.map((item,i)=><Link href={item.type==="projectLog"?`/writing/project/${item.slug}`:`/writing/${item.slug}`} key={`${item.type}-${item.slug}`}><span className="essayNo">{String(i+1).padStart(2,"0")}</span><h3>{item.title}</h3><p>{item.type==="projectLog"?String(item.meta.project):"Essay"}</p><time>{formatDate(item.date)}</time><b>↗</b></Link>)}</div></section><Footer/></main>}
