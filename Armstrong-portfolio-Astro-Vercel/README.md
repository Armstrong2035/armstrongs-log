# Armstrong Olusoji Portfolio

Astro version of Armstrong's portfolio, prepared for static deployment on Vercel.

```bash
npm install
npm run dev
npm run build
```

Vercel should detect Astro automatically. The build output is `dist`.
